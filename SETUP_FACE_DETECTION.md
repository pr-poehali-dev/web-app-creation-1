# 🎭 Настройка распознавания лиц для фотоальбома

## Быстрая установка

### Linux / macOS
```bash
bash scripts/download-face-models.sh
```

### Windows (PowerShell)
```powershell
powershell -ExecutionPolicy Bypass -File scripts/download-face-models.ps1
```

### Альтернатива: вручную через curl
```bash
cd public/models
curl -LO https://github.com/vladmandic/face-api/raw/master/model/tiny_face_detector_model-weights_manifest.json
curl -LO https://github.com/vladmandic/face-api/raw/master/model/tiny_face_detector_model-shard1
curl -LO https://github.com/vladmandic/face-api/raw/master/model/face_landmark_68_model-weights_manifest.json
curl -LO https://github.com/vladmandic/face-api/raw/master/model/face_landmark_68_model-shard1
```

## Что даст распознавание лиц?

### ✅ С моделями (рекомендуется)
- 👤 Автоматическое распознавание лиц на фотографиях
- 🎯 Умное размещение: лица НЕ попадают под корешок книги
- 📊 Статистика: количество найденных лиц
- 🟢 Зелёные рамки вокруг распознанных лиц
- ✨ Более качественная и продуманная вёрстка

### ⚠️ Без моделей (базовый режим)
- Фото размещаются случайным образом
- Лица могут попасть под корешок
- Базовая автовёрстка без учета содержимого фото

## Размер моделей

| Файл | Размер |
|------|--------|
| `tiny_face_detector_model-*` | ~1.1 MB |
| `face_landmark_68_model-*` | ~350 KB |
| **Итого** | **~1.5 MB** |

## Возможности редактора

### Автоматика
- ✅ Распознавание лиц при загрузке фото
- ✅ Умная вёрстка с учетом ориентации (горизонт/вертикаль)
- ✅ Защита лиц от корешка (синие линии)
- ✅ Максимальное заполнение белого поля

### Направляющие
- 🟡 **Желтая пунктирная рамка** - безопасная зона печати
- 🔵 **Синяя толстая линия** - корешок книги (центр)
- 🔵 **Синие пунктирные** - опасная зона для лиц (±15мм)
- ⚪ **Серая сетка** - вспомогательная сетка (вкл/выкл)
- 🟢 **Зелёные рамки** - обнаруженные лица

### Ручное редактирование
- 🖱️ **Drag & Drop** - перетаскивание фото
- 🔍 **Масштабирование** - кнопки +/- для выбранного фото
- 🎯 **Границы** - фото не выходят за желтые линии
- 🔄 **Перегенерация** - создать новый вариант вёрстки

## Проверка установки

После скачивания моделей:

1. Запустите проект: `npm run dev`
2. Откройте редактор фотоальбома
3. Загрузите фото с людьми
4. В консоли должно появиться: `✅ Face detection models loaded successfully`
5. После анализа увидите: `✅ N лиц защищено` (если лица найдены)

## Что делать если не работает?

### Проверьте структуру файлов
```
public/
└── models/
    ├── tiny_face_detector_model-weights_manifest.json
    ├── tiny_face_detector_model-shard1
    ├── face_landmark_68_model-weights_manifest.json
    └── face_landmark_68_model-shard1
```

### Проверьте консоль браузера (F12)
- ✅ `Face detection models loaded successfully` - модели загружены
- ⚠️ `Face detection models not found` - модели не найдены
- ⚠️ `Face detection will be disabled` - работа в базовом режиме

### Типичные проблемы

1. **"Failed to load models"**
   - Проверьте, что файлы находятся в `public/models/`
   - Проверьте, что файлы не повреждены

2. **"CORS error"**
   - Убедитесь, что dev-сервер запущен
   - Файлы должны быть в `public/`, а не в `src/`

3. **"0 лиц найдено"**
   - Возможно, на фото действительно нет лиц
   - Или лица слишком маленькие/повернуты
   - Модель работает лучше с четкими фронтальными портретами

## Технические детали

- **Библиотека**: `@vladmandic/face-api` v1.7.15
- **Модель детекции**: TinyFaceDetector (быстрая, легковесная)
- **Модель лендмарков**: FaceLandmark68Net
- **Threshold**: 0.5 (чувствительность распознавания)
- **Input size**: 224px (баланс скорость/точность)

## Источник моделей

Модели взяты из официального репозитория:
https://github.com/vladmandic/face-api/tree/master/model

Автор: Vladimir Mandic (@vladmandic)
Лицензия: MIT
