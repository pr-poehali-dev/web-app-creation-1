# Настройка Telegram-уведомлений

## Альтернатива Push-уведомлениям

Вместо Web Push API (который требует VAPID ключи) мы используем **Telegram Bot** для отправки уведомлений. Это бесплатно, просто и работает на всех устройствах.

## Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например: "ЕРТТП Уведомления")
4. Придумайте username бота (например: `erttp_notifications_bot`)
5. BotFather выдаст вам **токен бота** вида:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
6. Сохраните этот токен!

## Шаг 2: Добавьте токен в секреты проекта

1. Откройте настройки проекта в poehali.dev
2. Перейдите в раздел "Секреты"
3. Добавьте новый секрет:
   - Имя: `TELEGRAM_BOT_TOKEN`
   - Значение: ваш токен от BotFather

## Шаг 3: Как пользователи подключают уведомления

### Вариант А: Автоматическая привязка (рекомендуется)

Создайте страницу в приложении для привязки Telegram:

1. Пользователь нажимает "Подключить Telegram"
2. Открывается ссылка на вашего бота: `https://t.me/your_bot_name?start=USER_ID`
3. Пользователь нажимает "Start" в Telegram
4. Бот получает chat_id и автоматически привязывает к аккаунту

### Вариант Б: Ручная привязка (временное решение)

1. Пользователь открывает вашего бота в Telegram
2. Отправляет команду `/start`
3. Бот отвечает его `chat_id`
4. Пользователь вводит этот `chat_id` в профиле на сайте

## Шаг 4: Настройте Telegram бота (код бота)

Создайте простого бота, который будет получать chat_id пользователей:

```python
# telegram_bot.py
import os
from telegram import Update
from telegram.ext import Application, CommandHandler, ContextTypes

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    user = update.effective_user
    chat_id = update.effective_chat.id
    
    # Если пользователь пришел с параметром (user_id с сайта)
    if context.args:
        user_id = context.args[0]
        # Отправьте POST на /telegram-connect для привязки
        # ... код для отправки запроса
        await update.message.reply_text(
            f"✅ Telegram успешно подключен!\n\n"
            f"Теперь вы будете получать уведомления об откликах."
        )
    else:
        await update.message.reply_text(
            f"👋 Привет, {user.first_name}!\n\n"
            f"Ваш chat_id: `{chat_id}`\n\n"
            f"Скопируйте его и вставьте в настройках профиля на сайте."
        )

def main():
    token = os.environ['TELEGRAM_BOT_TOKEN']
    app = Application.builder().token(token).build()
    app.add_handler(CommandHandler("start", start))
    app.run_polling()

if __name__ == '__main__':
    main()
```

## API Endpoints

### 1. Проверка статуса подключения
```bash
GET https://functions.poehali.dev/e57f42ad-e2c3-420c-a418-16a98422a47d
Headers: X-User-Id: USER_ID
```

### 2. Привязка Telegram
```bash
POST https://functions.poehali.dev/e57f42ad-e2c3-420c-a418-16a98422a47d
Headers: X-User-Id: USER_ID
Body: { "telegram_chat_id": "123456789" }
```

### 3. Отправка уведомления
```bash
POST https://functions.poehali.dev/d49f8584-6ef9-47c0-9661-02560166e10f
Body: {
  "userId": "1",
  "title": "Новый отклик",
  "message": "Кто-то откликнулся на ваше предложение",
  "url": "/my-orders?id=123"
}
```

## Что уже работает

✅ Таблица `users` с колонкой `telegram_chat_id`  
✅ Backend функция для привязки Telegram (`telegram-connect`)  
✅ Backend функция для отправки уведомлений (`telegram-notify`)  
✅ Интеграция с OrderPage (отклики на предложения)  
✅ Интеграция с RequestDetail (отклики на запросы)

## Что нужно добавить

1. **UI для подключения Telegram** в профиле пользователя
2. **Telegram бота** для получения chat_id
3. **Токен бота** в секреты проекта

## Преимущества Telegram вместо Web Push

- ✅ Работает на всех устройствах (iOS, Android, Desktop)
- ✅ Не требует разрешений браузера
- ✅ Бесплатно и без лимитов
- ✅ Пользователи привыкли к Telegram
- ✅ Не нужны VAPID ключи
- ✅ Моментальная доставка

## Недостатки

- ❌ Требует у пользователя аккаунт в Telegram
- ❌ Нужно создать и поддерживать бота
