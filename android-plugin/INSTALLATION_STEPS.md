# 📋 Инструкция по установке обновлений

## Что добавлено

✅ **Установка иконки приложения** - твоя иконка с камерой  
✅ **Фильтрация фото по датам** - выбор фото с камеры по конкретной дате съёмки  
✅ **Чтение EXIF данных** - автоматическое определение даты съёмки из метаданных фото

---

## Шаг 1: Скопировать обновлённый плагин

Скопируй файл:

**ИЗ:** `C:\foto-mix.ru\foto-mix-android\android-plugin\CameraAccessPlugin.java`

**В:** `C:\foto-mix.ru\foto-mix-android\android\app\src\main\java\ru\fotomix\plugins\CameraAccessPlugin.java`

(Замени старый файл на новый)

---

## Шаг 2: Добавить зависимость для EXIF

Открой файл:
```
C:\foto-mix.ru\foto-mix-android\android\app\build.gradle
```

Найди секцию `dependencies {` и добавь эту строку:

```gradle
dependencies {
    // ... существующие зависимости ...
    
    // Для чтения EXIF данных из фото
    implementation 'androidx.exifinterface:exifinterface:1.3.6'
}
```

Сохрани файл.

---

## Шаг 3: Установить иконку приложения

### Автоматический способ (рекомендуется):

1. Открой сайт: https://romannurik.github.io/AndroidAssetStudio/icons-launcher.html

2. Загрузи файл: `C:\foto-mix.ru\foto-mix-android\android-plugin\icon.png`

3. Настрой параметры:
   - **Name:** `ic_launcher`
   - **Shape:** None (чтобы сохранить круглую форму)
   - **Background:** Transparent

4. Нажми **"Download"** и распакуй ZIP-архив

5. Скопируй все папки `mipmap-*` из архива в:
   ```
   C:\foto-mix.ru\foto-mix-android\android\app\src\main\res\
   ```
   (Замени существующие файлы)

---

## Шаг 4: Синхронизировать проект в Android Studio

1. Открой **Android Studio**
2. Нажми **File → Sync Project with Gradle Files**
3. Дождись завершения синхронизации

---

## Шаг 5: Пересобрать приложение

1. **Build → Clean Project**
2. **Build → Rebuild Project**
3. **Build → Generate App Bundles or APKs → Generate APKs**

---

## Шаг 6: Установить на телефон

После успешной сборки:
- Найди APK: `android\app\build\outputs\apk\debug\app-debug.apk`
- Удали старую версию приложения с телефона
- Установи новую версию

---

## ✨ Как использовать новые функции

### В JavaScript коде приложения:

```javascript
import { Plugins } from '@capacitor/core';
const { CameraAccess } = Plugins;

// 1. Получить список доступных дат с камеры
async function getPhotosDates() {
  const result = await CameraAccess.getAvailableDates();
  console.log('Доступные даты:', result.dates);
  // Вернёт: ["2024-01-15", "2024-01-14", "2024-01-10", ...]
}

// 2. Загрузить фото конкретной даты
async function loadPhotosByDate(selectedDate) {
  const result = await CameraAccess.pickFiles({
    filterDate: selectedDate // Например: "2024-01-15"
  });
  
  console.log('Фото за ' + selectedDate + ':', result.files);
  // Каждый файл теперь содержит поле "date"
}

// 3. Загрузить все фото без фильтра
async function loadAllPhotos() {
  const result = await CameraAccess.pickFiles();
  console.log('Все фото:', result.files);
}
```

---

## 🎯 Пример UI для выбора даты

Теперь можно создать интерфейс:

1. Кнопка "Посмотреть даты на камере" → вызывает `getAvailableDates()`
2. Показать список дат пользователю
3. Пользователь выбирает дату
4. Загружаются только фото этой даты через `pickFiles({ filterDate: '2024-01-15' })`

---

## 🐛 Проверка работы

После установки проверь:

1. ✅ Приложение открывается с новой иконкой
2. ✅ При подключении камеры можно выбрать несколько фото
3. ✅ В данных фото есть поле `date` с датой съёмки
4. ✅ Фильтр по дате работает корректно

---

## ❗ Важно

- EXIF данные читаются только из фото с камер/фотоаппаратов
- Скриншоты и загруженные из интернета фото могут не содержать EXIF
- Формат даты: `YYYY-MM-DD` (например: `2024-01-15`)
- Даты сортируются от новых к старым

---

Готово! Теперь приложение умеет фильтровать фото по датам! 🚀
